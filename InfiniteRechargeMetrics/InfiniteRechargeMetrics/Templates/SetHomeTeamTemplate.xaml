<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:InfiniteRechargeMetrics.Pages.Converters"
             mc:Ignorable="d"
             x:Class="InfiniteRechargeMetrics.Templates.SetHomeTeamTemplate">

    <StackLayout BackgroundColor="{StaticResource BackgroundColor}" Padding="50,100,50,50">
        <Label Text="Set Your Home Team" Style="{StaticResource CategoryHeader}"/>
        <Button Text="New" Style="{StaticResource HighLightButton}" Command="{Binding CreateNewTeamCMD}"/>
        <Button Text="Existing" Style="{StaticResource HighLightButton}" Command="{Binding UseExistingTeamCMD}"/>
        <Picker x:Name="TeamPicker" Title="Select A Team" SelectedIndexChanged="Picker_SelectedIndexChanged" IsVisible="{Binding IsUsingExisting}" TextColor="White"/>

        <Button Text="Save" IsVisible="{Binding IsUsingExisting}" IsEnabled="{Binding IsTeamSelected}" Style="{StaticResource HighLightButton}"/>
    </StackLayout>
    
</ContentView>