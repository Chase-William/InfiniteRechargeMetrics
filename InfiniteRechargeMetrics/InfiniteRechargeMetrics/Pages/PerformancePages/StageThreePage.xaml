<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local_templates="clr-namespace:InfiniteRechargeMetrics.Templates"
             mc:Ignorable="d"
             x:Class="InfiniteRechargeMetrics.Pages.PerformancePages.StageThreePage"
             Title="Stage"
             IconImageSource="stage_three.png">

    <ScrollView>
        <StackLayout BackgroundColor="{StaticResource BackgroundColor}" IsEnabled="{Binding StageCompletionManager.IsTimeOut, Converter={StaticResource BoolVisibilityConverter}}">
            <StackLayout Style="{StaticResource StageHeaderStackLayout}">
                <Label Text="Stage Status:" Style="{StaticResource DarkBGLabel}"/>
                <Label Text="{Binding StageCompletionManager.IsStageThreeComplete, Converter={StaticResource IsStageCompleteConverter}}" Style="{DynamicResource BaseLabel}" TextColor="{Binding StageCompletionManager.IsStageThreeComplete, Converter={StaticResource BoolToColorConverter}}"/>
            </StackLayout>

            <Grid IsEnabled="{Binding StageCompletionManager.IsStageTwoComplete}" Style="{StaticResource StageGrid}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="300"/>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="60"/>
                    <RowDefinition Height="60"/>
                    <RowDefinition Height="60"/>
                    <RowDefinition Height="60"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- The UI template for changing the points for a performance -->
                <local_templates:ChangePointsTemplate Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3"/>

                <Label Text="Droid Randevu" Style="{StaticResource CategoryHeader}" Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="1"/>
                <BoxView Grid.Row="2" Style="{StaticResource GridBasedHorizontalLineBreak}"/>
                
                <Label Text="Droid One (25pt):" Style="{StaticResource SwitchLabel}" Grid.Row="3"/>
                <Switch x:Name="DroidOneRandevuSwitch" x:FieldModifier="public" Style="{StaticResource StageSwitch}" Grid.Row="3"/>

                <Label Text="Droid Two (25pt):" Style="{StaticResource SwitchLabel}" Grid.Row="4"/>
                <Switch x:Name="DroidTwoRandevuSwitch" x:FieldModifier="public" Style="{StaticResource StageSwitch}" Grid.Row="4"/>

                <Label Text="Droid Three (5pt):" Style="{StaticResource SwitchLabel}" Grid.Row="5"/>
                <Switch x:Name="DroidThreeRandevuSwitch" x:FieldModifier="public" Style="{StaticResource StageSwitch}" Grid.Row="5"/>

                <Label Text="Is Randevu Bar Level:" Style="{StaticResource SwitchLabel}" Grid.Row="6"/>
                <Switch x:Name="IsRandevuBarLevelSwitch" x:FieldModifier="public" Style="{StaticResource StageSwitch}" Grid.Row="6"/>
            </Grid>
        </StackLayout>
    </ScrollView>
    
</ContentPage>